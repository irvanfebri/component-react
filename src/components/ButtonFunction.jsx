import React from "react";

// const ButtonFunction = (props) => {
//     return <button>{text ? text: "Default ButtonFunction"}</button>;

const ButtonFunction = ({text}) => {
    return <button>{text ? text: "Default ButtonFunction"}</button>;
    
    
};

export default ButtonFunction;